CONFIG_SPI=y                # Sharp 屏幕需要 SPI
CONFIG_SPI_NRF52=y          # nRF52840 特定 SPI 驱动
CONFIG_DISPLAY_SHARP_LS013B7DH03=y  # 启用 Sharp 驱动

# 通用显示配置
CONFIG_DISPLAY=y
CONFIG_DOUBLE_BUFFER=n      # Sharp 屏幕通常不需要双缓冲，节省 RAM
